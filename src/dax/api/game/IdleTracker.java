package dax.api.game;

import dax.api.utils.DaxObserver;

public class IdleTracker extends DaxObserver {
    @Override
    public int observe() throws InterruptedException {
        return 0;
    }
}
